version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - ./.env.local
    volumes:
      - ./app:/app
    command: yarn dev --host
    ports:
      - "3000:3000"